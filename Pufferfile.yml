puffers:
  http:
    services:
      airtable:
        baseurl: https://api.airtable.com
        routes:
          '*':
            headers: #inject or replace headers
              - "Authorization: Bearer keyWgchnviz5aPcR2"
            cache:
              expires: 0h10m00s
              maxsize: 20000000 #in bytes
              host: "localhost:6379"
              memcache: true #attention the memcache will ask the cache host first if no host is defined if it will request the url
          '/test':
            cache:
              expires: 1h0m0s
              maxsize: 20000000 #in bytes
              host: "redis:5234"
      myawesomeservice:
        baseurl: https://some.example.com
        routes:
          '*':
            headers:
              - "AKey: AValue"
            cache:
              expires: 0h2m00s
              maxsize: 20000000 #in bytes
              host: "localhost:6379"
          '/test':
            cache:
              expires: 1h0m0s
              maxsize: 20000000 #in bytes
              host: "redis:5234"